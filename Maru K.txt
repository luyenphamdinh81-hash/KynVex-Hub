-- Fluent UI
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
 -- Window Toggle + Roblox Image ID
local CoreGui = game:GetService("CoreGui")

if CoreGui:FindFirstChild("MaruWindow") then
    CoreGui.MaruWindow:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MaruWindow"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

-- Main Frame
local Frame = Instance.new("Frame")
Frame.Parent = ScreenGui
Frame.Size = UDim2.new(0, 420, 0, 260)
Frame.Position = UDim2.new(0.5, -210, 0.5, -130)
Frame.BackgroundColor3 = Color3.fromRGB(20,20,20)
Frame.BorderSizePixel = 0
Frame.Active = true
Frame.Draggable = true

-- Corner
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0,12)
Corner.Parent = Frame

-- Title
local Title = Instance.new("TextLabel")
Title.Parent = Frame
Title.Size = UDim2.new(1,0,0,40)
Title.BackgroundTransparency = 1
Title.Text = "Maru Hub"
Title.TextColor3 = Color3.new(1,1,1)
Title.TextScaled = true
Title.Font = Enum.Font.GothamBold

-- Roblox Image
local Image = Instance.new("ImageLabel")
Image.Parent = Frame
Image.Size = UDim2.new(0,120,0,120)
Image.Position = UDim2.new(0.5,-60,0.5,-40)
Image.BackgroundTransparency = 1

-- Thay ID Roblox tại đây
Image.Image = "rbxassetid://1234567890"

-- Toggle Button
local Toggle = Instance.new("TextButton")
Toggle.Parent = ScreenGui
Toggle.Size = UDim2.new(0,60,0,60)
Toggle.Position = UDim2.new(0,20,0.5,-30)
Toggle.Text = "ON/OFF"
Toggle.TextScaled = true
Toggle.BackgroundColor3 = Color3.fromRGB(30,30,30)
Toggle.TextColor3 = Color3.new(1,1,1)
Toggle.Draggable = true

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(1,0)
ToggleCorner.Parent = Toggle

Toggle.MouseButton1Click:Connect(function()
    Frame.Visible = not Frame.Visible
end)
-- Window
local Window = Fluent:CreateWindow({
    Title = "Maru Hub [ BloxFruits ]",
    SubTitle = "Maru Premium",
    TabWidth = 140,
    Size = UDim2.fromOffset(520, 360),
    Acrylic = true,
    Theme = "Aqua",
    MinimizeKey = Enum.KeyCode.LeftControl
})

-- Tabs
local MainTab = Window:AddTab({ Title = "Main" })
local SettingTab = Window:AddTab({ Title = "Setting" })
local FightingTab = Window:AddTab({ Title = "Fighting" })
local ItemsTab = Window:AddTab({ Title = "Items" })
local SeaEventsTab = Window:AddTab({ Title = "Sea Events" })
local MirageTab = Window:AddTab({ Title = "Mirage + Race V4" })
local DragonTab = Window:AddTab({ Title = "Dragon Event" })
local PrehistoricTab = Window:AddTab({ Title = "Prehistoric Island" })
local DungeonTab = Window:AddTab({ Title = "Dungeon" })
local PvpTab = Window:AddTab({ Title = "PVP" })
local TeleportTab = Window:AddTab({ Title = "Teleport" })
local FruitsTab = Window:AddTab({ Title = "Fruits" })
local ShopTab = Window:AddTab({ Title = "Shop" })
local MiscTab = Window:AddTab({ Title = "Misc" })

-- Maru Info
MainTab:AddParagraph({
    Title = "Maru Hub",
    Content = "Premium Version Loaded"
})

-- Notify
Fluent:Notify({
    Title = "Maru Hub",
    Content = "Maru Premium UI Loaded",
    Duration = 3
})
MainTab:AddSection("Main Farm")

-- FIX RUNG LẮC KHI ĐỨNG TRÊN ĐẦU QUÁI
-- Thay đoạn teleport cũ bằng đoạn này

local Offset = CFrame.new(0, 25, 0)

task.spawn(function()
    while task.wait() do
        pcall(function()
            if _G.AutoFarm then
                local char = LocalPlayer.Character
                if not char then return end

                local hrp = char:FindFirstChild("HumanoidRootPart")
                local humanoid = char:FindFirstChild("Humanoid")

                if not hrp or not humanoid then return end

                local mob = GetClosestMob()

                if mob and mob:FindFirstChild("HumanoidRootPart") then
                    local mobHRP = mob.HumanoidRootPart

                    -- Fix rung bằng PivotTo
                    char:PivotTo(mobHRP.CFrame * Offset)

                    -- Khoá vật lý hoàn toàn
                    hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    hrp.AssemblyAngularVelocity = Vector3.new(0,0,0)

                    -- Tắt rơi + rung
                    humanoid.PlatformStand = true
                    humanoid.AutoRotate = false
                end
            else
                local char = LocalPlayer.Character
                if char and char:FindFirstChild("Humanoid") then
                    char.Humanoid.PlatformStand = false
                    char.Humanoid.AutoRotate = true
                end
            end
        end)
    end
end)

-- Heartbeat Fix Mượt
RunService.RenderStepped:Connect(function()
    if _G.AutoFarm then
        local char = LocalPlayer.Character

        if char and char:FindFirstChild("HumanoidRootPart") then
            local hrp = char.HumanoidRootPart

            hrp.Velocity = Vector3.zero
            hrp.RotVelocity = Vector3.zero
        end
    end
end)
MainTab:AddToggle("FarmLevel", {
    Title = "Farm Level",
    Default = false,
    Callback = function(Value)
        if Value then
            Fluent:Notify({
                Title = "Farm Level",
                Content = "Farm Level Enabled",
                Duration = 3
            })

            -- Put your farm system here

        else
            Fluent:Notify({
                Title = "Farm Level",
                Content = "Farm Level Disabled",
                Duration = 3
            })
        end
    end
})

MainTab:AddParagraph({
    Title = "Farm System",
    Content = "Attach your own quest + mob logic here"
})
-- Toggle UI Button (Đóng/Mở Menu)
-- Có chỗ thay ID ảnh Roblox

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")

local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

-- GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ToggleMenuUI"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false

-- Button Toggle
local ToggleButton = Instance.new("ImageButton")
ToggleButton.Parent = ScreenGui
ToggleButton.Size = UDim2.new(0, 60, 0, 60)
ToggleButton.Position = UDim2.new(0, 20, 0.5, -30)
ToggleButton.BackgroundTransparency = 1
ToggleButton.Image = "rbxassetid://1234567890" -- THAY ID ẢNH ROBLOX Ở ĐÂY
ToggleButton.Active = true
ToggleButton.Draggable = true

-- Bo góc
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(1,0)
UICorner.Parent = ToggleButton

-- Menu cần đóng mở
local MenuFrame = Instance.new("Frame")
MenuFrame.Parent = ScreenGui
MenuFrame.Size = UDim2.new(0, 500, 0, 350)
MenuFrame.Position = UDim2.new(0.5, -250, 0.5, -175)
MenuFrame.BackgroundColor3 = Color3.fromRGB(25,25,25)
MenuFrame.Visible = true

local MenuCorner = Instance.new("UICorner")
MenuCorner.CornerRadius = UDim.new(0,12)
MenuCorner.Parent = MenuFrame

-- Drag Menu
local dragging = false
local dragInput, dragStart, startPos

MenuFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MenuFrame.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

MenuFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

UIS.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MenuFrame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

-- Toggle đóng mở
ToggleButton.MouseButton1Click:Connect(function()
    MenuFrame.Visible = not MenuFrame.Visible
end)
-- Auto Farm Level 1 -> 2800
local AutoFarm = false

MainTab:AddToggle("AutoFarmLevel", {
    Title = "Farm Level 1-2800",
    Default = false,
    Callback = function(Value)
        AutoFarm = Value

        task.spawn(function()
            while AutoFarm do
                pcall(function()
                    local Player = game.Players.LocalPlayer
                    local Character = Player.Character
                    local HRP = Character and Character:FindFirstChild("HumanoidRootPart")
                    local Level = Player.Data.Level.Value

                    if not HRP then return end

                    -- Quest + Mob Table
                    local QuestData = {
                        {Min=1, Max=9, Quest="BanditQuest1", LevelQuest=1, Mob="Bandit", QuestPos=CFrame.new(1060,17,1547)},
                        {Min=10, Max=14, Quest="JungleQuest", LevelQuest=1, Mob="Monkey", QuestPos=CFrame.new(-1602,37,152)},
                        {Min=15, Max=29, Quest="JungleQuest", LevelQuest=2, Mob="Gorilla", QuestPos=CFrame.new(-1237,7,-486)},
                        {Min=30, Max=39, Quest="BuggyQuest1", LevelQuest=1, Mob="Pirate", QuestPos=CFrame.new(-1140,5,3828)},
                        {Min=40, Max=59, Quest="BuggyQuest1", LevelQuest=2, Mob="Brute", QuestPos=CFrame.new(-1140,5,3828)},
                        {Min=60, Max=74, Quest="DesertQuest", LevelQuest=1, Mob="Desert Bandit", QuestPos=CFrame.new(896,6,4390)},
                        {Min=75, Max=89, Quest="DesertQuest", LevelQuest=2, Mob="Desert Officer", QuestPos=CFrame.new(896,6,4390)},
                        {Min=90, Max=99, Quest="SnowQuest", LevelQuest=1, Mob="Snow Bandit", QuestPos=CFrame.new(1389,87,-1298)},
                        {Min=100, Max=119, Quest="SnowQuest", LevelQuest=2, Mob="Snowman", QuestPos=CFrame.new(1389,87,-1298)},
                        {Min=120, Max=149, Quest="MarineQuest2", LevelQuest=1, Mob="Chief Petty Officer", QuestPos=CFrame.new(-5035,29,4325)},
                        {Min=150, Max=189, Quest="SkyQuest", LevelQuest=1, Mob="Sky Bandit", QuestPos=CFrame.new(-4842,717,-2623)},
                        {Min=190, Max=209, Quest="PrisonerQuest", LevelQuest=1, Mob="Prisoner", QuestPos=CFrame.new(5308,2,474)},
                        {Min=210, Max=249, Quest="ColosseumQuest", LevelQuest=1, Mob="Toga Warrior", QuestPos=CFrame.new(-1577,7,-2984)},
                        {Min=250, Max=299, Quest="ColosseumQuest", LevelQuest=2, Mob="Gladiator", QuestPos=CFrame.new(-1577,7,-2984)},
                        {Min=300, Max=374, Quest="MagmaQuest", LevelQuest=1, Mob="Military Soldier", QuestPos=CFrame.new(-5316,12,8517)},
                        {Min=375, Max=449, Quest="FishmanQuest", LevelQuest=1, Mob="Fishman Warrior", QuestPos=CFrame.new(61122,18,1568)},
                        {Min=450, Max=624, Quest="SkyExp1Quest", LevelQuest=1, Mob="God's Guard", QuestPos=CFrame.new(-4722,845,-1954)},
                        {Min=625, Max=699, Quest="FountainQuest", LevelQuest=1, Mob="Galley Pirate", QuestPos=CFrame.new(5258,39,4050)},
                        {Min=700, Max=799, Quest="Area1Quest", LevelQuest=1, Mob="Raider", QuestPos=CFrame.new(-429,73,1837)},
                        {Min=800, Max=899, Quest="Area1Quest", LevelQuest=2, Mob="Mercenary", QuestPos=CFrame.new(-429,73,1837)},
                        {Min=900, Max=999, Quest="MarineQuest3", LevelQuest=1, Mob="Marine Captain", QuestPos=CFrame.new(-2440,73,-3217)},
                        {Min=1000, Max=1099, Quest="SnowMountainQuest", LevelQuest=1, Mob="Snow Trooper", QuestPos=CFrame.new(605,402,-5371)},
                        {Min=1100, Max=1199, Quest="IceSideQuest", LevelQuest=1, Mob="Lab Subordinate", QuestPos=CFrame.new(-6060,16,-4905)},
                        {Min=1200, Max=1299, Quest="FireSideQuest", LevelQuest=1, Mob="Magma Ninja", QuestPos=CFrame.new(-5430,16,-5295)},
                        {Min=1300, Max=1399, Quest="ShipQuest1", LevelQuest=1, Mob="Ship Deckhand", QuestPos=CFrame.new(1037,125,32911)},
                        {Min=1400, Max=1499, Quest="FrostQuest", LevelQuest=1, Mob="Arctic Warrior", QuestPos=CFrame.new(5668,28,-6484)},
                        {Min=1500, Max=1599, Quest="PiratePortQuest", LevelQuest=1, Mob="Pirate Millionaire", QuestPos=CFrame.new(-290,44,5580)},
                        {Min=1600, Max=1699, Quest="AmazonQuest", LevelQuest=1, Mob="Dragon Crew Warrior", QuestPos=CFrame.new(5833,52,-1103)},
                        {Min=1700, Max=1799, Quest="MarineTreeIsland", LevelQuest=1, Mob="Marine Commodore", QuestPos=CFrame.new(2180,29,-6737)},
                        {Min=1800, Max=1899, Quest="DeepForestIsland3", LevelQuest=1, Mob="Fishman Raider", QuestPos=CFrame.new(-10581,332,-8758)},
                        {Min=1900, Max=1999, Quest="DeepForestIsland", LevelQuest=1, Mob="Jungle Pirate", QuestPos=CFrame.new(-13232,332,-7627)},
                        {Min=2000, Max=2099, Quest="HauntedQuest1", LevelQuest=1, Mob="Living Zombie", QuestPos=CFrame.new(-9480,142,5566)},
                        {Min=2100, Max=2199, Quest="NutsIslandQuest", LevelQuest=1, Mob="Peanut Scout", QuestPos=CFrame.new(-2104,38,-10194)},
                        {Min=2200, Max=2299, Quest="IceCreamIslandQuest", LevelQuest=1, Mob="Ice Cream Chef", QuestPos=CFrame.new(-821,66,-10965)},
                        {Min=2300, Max=2399, Quest="CakeQuest1", LevelQuest=1, Mob="Cookie Crafter", QuestPos=CFrame.new(-2022,37,-12028)},
                        {Min=2400, Max=2499, Quest="CakeQuest2", LevelQuest=1, Mob="Candy Rebel", QuestPos=CFrame.new(-1147,15,-14446)},
                        {Min=2500, Max=2800, Quest="TikiQuest1", LevelQuest=1, Mob="Island Boy", QuestPos=CFrame.new(-16545,56,-172)}
                    }

                    for _,v in pairs(QuestData) do
                        if Level >= v.Min and Level <= v.Max then
                            HRP.CFrame = v.QuestPos
                            task.wait(1)

                            for _,Enemy in pairs(workspace.Enemies:GetChildren()) do
                                if Enemy.Name == v.Mob and Enemy:FindFirstChild("Humanoid") and Enemy.Humanoid.Health > 0 then
                                    repeat
                                        HRP.CFrame = Enemy.HumanoidRootPart.CFrame * CFrame.new(0,8,0)
                                        task.wait(0.15)
                                    until Enemy.Humanoid.Health <= 0 or not AutoFarm
                                end
                            end
                        end
                    end
                end)
                task.wait()
            end
        end)
    end
})
-- =========================
-- FARM BONE + FARM KATAKURI
-- Gắn trong Tab Main
-- =========================

local BoneFarm = false
local KatakuriFarm = false

-- Farm Bone
MainTab:AddToggle("FarmBone", {
    Title = "Farm Bone",
    Default = false,
    Callback = function(Value)
        BoneFarm = Value

        task.spawn(function()
            while BoneFarm do
                pcall(function()
                    local Player = game.Players.LocalPlayer
                    local Character = Player.Character
                    local HRP = Character and Character:FindFirstChild("HumanoidRootPart")

                    if not HRP then return end

                    -- Teleport tới Haunted Castle
                    HRP.CFrame = CFrame.new(-9515, 142, 5567)

                    for _, Enemy in pairs(workspace.Enemies:GetChildren()) do
                        if Enemy:FindFirstChild("Humanoid")
                        and Enemy:FindFirstChild("HumanoidRootPart")
                        and Enemy.Humanoid.Health > 0 then

                            if Enemy.Name == "Living Zombie"
                            or Enemy.Name == "Demonic Soul"
                            or Enemy.Name == "Posessed Mummy" then

                                repeat
                                    task.wait()

                                    HRP.CFrame = Enemy.HumanoidRootPart.CFrame * CFrame.new(0,8,0)

                                    game:GetService("VirtualUser"):Button1Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
                                until Enemy.Humanoid.Health <= 0 or not BoneFarm
                            end
                        end
                    end
                end)

                task.wait(0.2)
            end
        end)
    end
})

-- Farm Katakuri / Dough King
MainTab:AddToggle("FarmKatakuri", {
    Title = "Farm Katakuri",
    Default = false,
    Callback = function(Value)
        KatakuriFarm = Value

        task.spawn(function()
            while KatakuriFarm do
                pcall(function()
                    local Player = game.Players.LocalPlayer
                    local Character = Player.Character
                    local HRP = Character and Character:FindFirstChild("HumanoidRootPart")

                    if not HRP then return end

                    for _, Boss in pairs(workspace.Enemies:GetChildren()) do
                        if Boss.Name == "Dough King"
                        or Boss.Name == "Cake Prince" then

                            if Boss:FindFirstChild("Humanoid")
                            and Boss:FindFirstChild("HumanoidRootPart")
                            and Boss.Humanoid.Health > 0 then

                                repeat
                                    task.wait()

                                    HRP.CFrame = Boss.HumanoidRootPart.CFrame * CFrame.new(0,25,0)

                                    game:GetService("VirtualUser"):Button1Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
                                until Boss.Humanoid.Health <= 0 or not KatakuriFarm
                            end
                        end
                    end
                end)

                task.wait(0.5)
            end
        end)
    end
})
-- =========================
-- AUTO ATTACK GẦN + CÂU CÁ + MUA MỒI
-- BLACKBEARD + RIP_INDRA
-- Gắn vào MainTab
-- =========================

local AttackNear = false
local AutoFish = false
local AutoBuyBait = false
local AutoBlackbeard = false
local AutoRipIndra = false

-- =========================
-- ĐÁNH QUÁI GẦN
-- =========================
MainTab:AddToggle("AttackNear", {
    Title = "Attack Quái Gần",
    Default = false,
    Callback = function(Value)
        AttackNear = Value

        task.spawn(function()
            while AttackNear do
                pcall(function()
                    local Player = game.Players.LocalPlayer
                    local Char = Player.Character
                    local HRP = Char and Char:FindFirstChild("HumanoidRootPart")

                    if not HRP then return end

                    for _,Enemy in pairs(workspace.Enemies:GetChildren()) do
                        if Enemy:FindFirstChild("Humanoid")
                        and Enemy:FindFirstChild("HumanoidRootPart")
                        and Enemy.Humanoid.Health > 0 then

                            local Distance = (Enemy.HumanoidRootPart.Position - HRP.Position).Magnitude

                            if Distance <= 60 then
                                HRP.CFrame = Enemy.HumanoidRootPart.CFrame * CFrame.new(0,8,0)

                                game:GetService("VirtualUser"):Button1Down(
                                    Vector2.new(0,0),
                                    workspace.CurrentCamera.CFrame
                                )
                            end
                        end
                    end
                end)

                task.wait(0.15)
            end
        end)
    end
})

-- =========================
-- AUTO MUA MỒI
-- =========================
MainTab:AddToggle("AutoBuyBait", {
    Title = "Auto Buy Bait",
    Default = false,
    Callback = function(Value)
        AutoBuyBait = Value

        task.spawn(function()
            while AutoBuyBait do
                pcall(function()
                    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(
                        "BuyFishBait",
                        "Basic Bait"
                    )
                end)

                task.wait(10)
            end
        end)
    end
})

-- =========================
-- AUTO CÂU CÁ
-- =========================
MainTab:AddToggle("AutoFish", {
    Title = "Auto Fish",
    Default = false,
    Callback = function(Value)
        AutoFish = Value

        task.spawn(function()
            while AutoFish do
                pcall(function()
                    local Tool = game.Players.LocalPlayer.Character:FindFirstChild("Fishing Rod")

                    if Tool then
                        Tool:Activate()
                    end
                end)

                task.wait(2)
            end
        end)
    end
})

-- =========================
-- AUTO BLACKBEARD
-- =========================
MainTab:AddToggle("AutoBlackbeard", {
    Title = "Farm Blackbeard",
    Default = false,
    Callback = function(Value)
        AutoBlackbeard = Value

        task.spawn(function()
            while AutoBlackbeard do
                pcall(function()
                    local HRP = game.Players.LocalPlayer.Character.HumanoidRootPart

                    for _,Boss in pairs(workspace.Enemies:GetChildren()) do
                        if Boss.Name == "Darkbeard"
                        and Boss:FindFirstChild("Humanoid")
                        and Boss.Humanoid.Health > 0 then

                            repeat
                                task.wait()

                                HRP.CFrame = Boss.HumanoidRootPart.CFrame * CFrame.new(0,25,0)

                                game:GetService("VirtualUser"):Button1Down(
                                    Vector2.new(0,0),
                                    workspace.CurrentCamera.CFrame
                                )
                            until Boss.Humanoid.Health <= 0 or not AutoBlackbeard
                        end
                    end
                end)

                task.wait(0.2)
            end
        end)
    end
})

-- =========================
-- AUTO RIP_INDRA
-- =========================
MainTab:AddToggle("AutoRipIndra", {
    Title = "Farm Rip Indra",
    Default = false,
    Callback = function(Value)
        AutoRipIndra = Value

        task.spawn(function()
            while AutoRipIndra do
                pcall(function()
                    local HRP = game.Players.LocalPlayer.Character.HumanoidRootPart

                    for _,Boss in pairs(workspace.Enemies:GetChildren()) do
                        if Boss.Name == "rip_indra True Form"
                        or Boss.Name == "rip_indra" then

                            if Boss:FindFirstChild("Humanoid")
                            and Boss.Humanoid.Health > 0 then

                                repeat
                                    task.wait()

                                    HRP.CFrame = Boss.HumanoidRootPart.CFrame * CFrame.new(0,30,0)

                                    game:GetService("VirtualUser"):Button1Down(
                                        Vector2.new(0,0),
                                        workspace.CurrentCamera.CFrame
                                    )
                                until Boss.Humanoid.Health <= 0 or not AutoRipIndra
                            end
                        end
                    end
                end)

                task.wait(0.3)
            end
        end)
    end
})